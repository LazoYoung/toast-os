plugins {
    id 'java'
    id 'application'
}

application {
    mainClassName = 'toast.Main'
}

tasks.jar {
    manifest {
        attributes['Main-Class'] = 'toast.Main'
    }
}

run {
    // Fix ./gradlew run
    standardInput = System.in
}

repositories {
    mavenCentral()
}

test {
    useJUnitPlatform()
}
